
@model IEnumerable<MvcBookStore.Models.NHASANXUAT>
<p class="lead"> NHÀ SẢN XUẤT</p>
<div class="list-group">
    @foreach (var item in Model)
    {
        <a href="@Url.Action("SPTheoNSX","ShopGiay",new { id = item.MaNSX})" class="list-group-item">
            @item.TenNSX
        </a>
    }
</div>

